#include "main.h"
/**
 * sub - subtracts two integers
 * @a: int to be subtracted to b
 * @b: int to be subtracted to a
 * Return: the result of the operation
 */
int sub(int a, int b)
{
	int c;

	c = a - b;
	return (c);
}
